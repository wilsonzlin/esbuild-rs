{
  "name": "esbuild-native",
  "private": true,
  "main": "dist/index.js",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && neon build --release && mv native/index.node dist/esbuild.node"
  },
  "dependencies": {
    "@types/node": "^14.0.22",
    "neon-cli": "^0.4.0",
    "typescript": "^3.9.6"
  }
}
