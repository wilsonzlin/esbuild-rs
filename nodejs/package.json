{
  "name": "esbuild-native",
  "private": true,
  "main": "dist/index.js",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build-native-init": "node-gyp configure",
    "build-native": "./build-go.sh && node-gyp build",
    "build": "tsc && npm run build-native && mv build/Release/binding.node dist/native.node"
  },
  "dependencies": {
    "@types/node": "^14.0.22",
    "node-gyp": "^7.0.0",
    "typescript": "^3.9.6"
  }
}
